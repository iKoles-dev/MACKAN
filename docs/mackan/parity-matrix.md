# MACKAN Feature Parity Matrix

Status values:

- `required`: must exist before 1.0
- `better-native`: replace Windows UI pattern with a native macOS equivalent
- `blocked`: needs upstream/Core work before final implementation

| Area | Windows CKAN capability | MACKAN requirement | Status |
| --- | --- | --- | --- |
| App launch | Default GUI on Windows | Native macOS app launch without Terminal | completed |
| Existing macOS package | ConsoleUI via Terminal | Replace with native GUI, keep CLI as fallback only | completed |
| Instances | Manage game instances | Native instance sidebar/menu plus Manage Instances sheet with add/clone/fake entry points, selected/default/invalid status, rename/default/forget/reveal/select actions, and CKAN Core-backed operation state; disposable-instance coverage verifies add, clone, fake, rename, set-default, and remove flows against real KSP folders | completed |
| Instances | Add instance | Instance menu and Manage Instances sheet entry point open a native Finder folder picker/name sheet backed by CKAN Core `GameInstanceManager.AddInstance` through `instances.add`; disposable-instance coverage verifies registering a real KSP folder path into CKAN config | completed |
| Instances | Clone instance | Instance menu and Manage Instances sheet entry point open a native source picker, destination folder, name, stock-folder symlink option, optional-path leave-empty toggles from `instances.cloneOptions`, and CKAN Core `GameInstanceManager.CloneInstance` through `instances.clone`; disposable-instance coverage verifies cloned KSP folders and copied configured launch options | completed |
| Instances | Rename instance | Sidebar context action and Manage Instances sheet rename action implemented through CKAN Core `GameInstanceManager.RenameInstance`; disposable-instance coverage verifies renamed config entries preserve the original game path | completed |
| Instances | Forget/remove instance | Sidebar confirmation and Manage Instances sheet confirmation remove from CKAN config only through CKAN Core `GameInstanceManager.RemoveInstance`; disposable-instance coverage verifies removal from config without depending on synthetic provider state | completed |
| Instances | Fake instance | Instance menu and Manage Instances sheet entry point open an advanced sheet that creates KSP/KSP2 test/dev folders through CKAN Core `GameInstanceManager.FakeInstance`, with game version, optional KSP DLC versions, and set-default flag; disposable-instance coverage verifies created KSP folder structure, DLC folders, and default selection | completed |
| Instances | Set default instance | Sidebar context action, Instance menu command, and Manage Instances sheet action implemented through CKAN Core `GameInstanceManager.SetAutoStart` | completed |
| Instances | Open game directory | Sidebar Finder reveal action, Instance menu command, main toolbar command, and Manage Instances sheet reveal action implemented | better-native |
| Instances | Launch game | Instance menu command, configured command-line submenu, editing sheet, Settings Launch pane, toolbar command, incompatible-mod warning sheet, and suppression flag implemented; launch result includes `processId` for immediate process tracking and explicit launch-failure errors when a configured command cannot be started | completed |
| Repositories | Refresh/update repo data | Settings-pane refresh plus main toolbar and Mods menu refresh implemented with summary and CKAN Core message/progress event timeline; refresh can run as `repositories.startRefresh` with `repositories.refreshStatus` polling, `repositories.cancelRefresh`, native auto-polling, and visible running/cancelling state; repository metadata download failures now return typed `downloadFailures` details with repository name, metadata version marker, URL, error message, retry/edit guidance, and a native retry notice | completed |
| Repositories | Add/remove/move repositories | Repository settings table has model-backed row action state and priority controls for add/remove/move up/move down; sidecar methods `repositories.add`, `repositories.remove`, and `repositories.setPriority` are covered from Swift client through dispatcher and CKAN Core; disposable-instance coverage verifies list/add/remove/priority persistence against a real CKAN registry; native UI exposes Add, Remove, Move Up, and Move Down actions with row-level enablement | completed |
| Repositories | Canonical repository list | `repositories.available` loads selected-game CKAN Core canonical sources through `RepositoryList.DefaultRepositories`; Swift client and AppModel preserve selected-instance source lists, and the Add Repository sheet seeds name/URL fields from selected known sources before adding | completed |
| Catalog | Full mod list | Native catalog grid backed by CKAN Core module summaries with explicit catalog flags for installed, compatible, cached, new, update, replacement, auto-installed, autodetected, compatibility, size, date, and download-count state; persistent Columns menu can show/hide status, pending, auto, name, identifier, installed/latest, author, compatibility, size, date, downloads, license, tags, and description columns; search/filter/tag/sort state survives relaunch; installed non-autodetected modules can toggle auto-installed state through CKAN Core | completed |
| Catalog | Installed filter | Native filter menu includes Installed and Not Installed | better-native |
| Catalog | Upgradable filter | Native filter menu includes Upgradable and toolbar Upgrade All badge/action | better-native |
| Catalog | Compatible/incompatible filters | Native filter menu includes Compatible and Incompatible via `ModuleFilter.allCases`; AppModel filters against explicit `mods.list` `isCompatible` flags decoded by the Swift sidecar client | completed |
| Catalog | Replaceable filter | Native filter menu includes Replaceable; CKAN Core computes `hasReplacement`, `mods.list` transports it, and AppModel only includes installed replaceable modules | completed |
| Catalog | Cached/uncached filters | Native filter menu includes Cached and Uncached; CKAN Core computes `isCached`, `mods.list` transports it, and AppModel applies both states directly | completed |
| Catalog | New modules filter | Native filter menu includes New; repository refresh marks newly indexed and newly compatible modules, then AppModel filters on `isNew` | completed |
| Catalog | Search | Tokenized native search matches name, identifier, author, status, versions, license, relationships, contents, tags, CKAN Core labels, descriptions, and localizations; Windows-style advanced tokens cover `@author`, `desc:`, `lic:`, `lang:`, `dep:`, `rec:`, `sug:`, `conf:`, `sup:`, `tag:`, `label:`, and `is:`/`not:` state filters with negated scoped terms; saved searches can be saved/applied/deleted through a persistent native menu; a native Search Syntax menu documents and inserts supported advanced queries | completed |
| Catalog | Column sorting | Explicit native sort menu and clickable table headers cover name, identifier, status, installed/latest version, author, compatibility, download/install size, release/install date, and download count with ascending/descending toggle; a native Then By menu adds persistent chained secondary sort criteria with per-column direction and clear/remove controls | better-native |
| Catalog | Labels/tags | Metadata tags are exposed in module summaries, searchable, and filterable through a native Tag menu; CKAN Core custom labels are loaded, searchable through normalized `label:`, toggleable for the selected module, listed in the native sidebar, and manageable through a native Labels sheet with create/edit/delete/color/scope/Windows flag support; the label contract now separates selected-instance catalog labels from cross-instance manageable labels so labels scoped to other instances remain editable without polluting catalog search/filter state | completed |
| Mod details | Metadata tab | Inspector Overview tab implemented | completed |
| Mod details | Relationships tab | Inspector Relationships tab implemented as a native relationship graph/tree with central module, relationship-kind badges, related nodes, and accessible combined rows; richer dependency resolution metadata pending | completed |
| Mod details | Versions tab | Inspector Versions tab implemented as a native list | completed |
| Mod details | Contents tab | Inspector Contents tab implemented as a native path list | completed |
| Mod details | Resources/homepage/license | Inspector Resources tab implemented with selectable URLs | completed |
| Change set | Install module | Selection state, preview, async start/status/cancel apply loop, and direct CKAN Core disposable apply are implemented. Provider alternatives are surfaced in preview, selected as first-class `providerSelections`, forwarded through AppModel/Sidecar apply, and applied without staging the provider as a direct user install. | completed |
| Change set | Remove module | Selection state, preview, async start/status/cancel apply loop, and direct CKAN Core disposable remove/apply are implemented with registry mutation verification. | completed |
| Change set | Upgrade selected/all | Toolbar and Mods menu commands share tested upgradable-module readiness; staging, preview, async start/status/cancel apply loop, and direct CKAN Core disposable upgrade/apply are implemented. Injected repository sources are preserved across preview/apply so upgradeability checks see the same available metadata. | completed |
| Change set | Replace module | Replaceable installed modules can stage replacement from the native selected-module action menu; sidecar `replace` selections resolve to replace/install preview rows and apply through the same async Apply Changes path; disposable CKAN Core coverage verifies registry mutation from replaceable module to replacement module, and replace planning no longer emits duplicate auto-remove summaries. | completed |
| Change set | Dependency choices | CKAN Core multi-provider dependency choices are surfaced as a native dependency-choice panel in the preview sheet; Apply stays blocked until the user selects one provider per dependency, the selection is re-resolved through `providerSelections`, and AppModel/Sidecar/Core apply coverage verifies the selected providers are forwarded and installed. | completed |
| Change set | Recommendations | Sidecar preview exposes optional recommendations, suggestions, and supporters as `recommendationChoices`; native preview shows them without blocking Apply, can stage an optional mod for the next preview pass, and can toggle the Windows-compatible persistent `SuppressRecommendations` preference in `GUIConfig.json`; disposable CKAN Core coverage verifies recommendation preview choices and selected recommendation apply through the normal staged install path. | completed |
| Change set | Provides alternatives | Sidecar preview exposes CKAN Core `TooManyModsProvideKraken` alternatives as `providerChoices`; native preview blocks Apply until the user selects an option, persists that choice as `providerSelections`, and re-resolves/applies it without treating the provider as a direct user-requested install. Core operation apply returns typed `providerChoices` when unresolved and installs the selected provider when provided. | completed |
| Change set | Conflict handling | Native preview blocks Apply for CKAN Core conflicts and shows a dedicated conflict panel with selected-conflict rows, raw conflict descriptions, and recovery guidance to change selection and re-preview; disposable CKAN Core and AppModel coverage verify conflict summaries, raw conflict descriptions, and Apply readiness blocking. | completed |
| Operations | Download progress | Operation timeline with per-file download/store/install events, percent, identifier, and byte progress; status polling is implemented for Apply Changes, `.ckan` install, import-download install, and repository refresh workflows; failed downloads surface typed `downloadFailures` details with failed module, version, URL, error message, and retry/skip recovery. Disposable Core coverage verifies real CKAN download/store/install progress events for Apply Changes and `.ckan`; dispatcher, Sidecar, AppModel, diagnostics, and native operation sheet coverage preserve and render the specialized event payload. Prompt-heavy push streaming remains a post-v1 improvement because v1 uses status polling. | completed |
| Operations | Install progress | Operation timeline shows install/remove/download/store phases with cancellable start/status/cancel loops where safe; AppModel stores running/completed operation snapshots, the native sheet auto-polls active operations, and progress events include kind labels, percent bars, identifiers, and byte counts. Disposable Core coverage verifies real `installProgress` events for Apply Changes and `.ckan`; dispatcher, Sidecar, and AppModel coverage verifies specialized event kinds survive status polling. | completed |
| Operations | Registry lock handling | Sidecar maps `RegistryInUseKraken` to typed `registryLock` details in JSON-RPC errors and operation results; preview and operation sheets show the lock path, Retry, and an explicit confirmed Remove Lock File action for stale `registry.locked` files; disposable CKAN Core coverage verifies live `registry.locked` handling for Apply Changes, `.ckan` install, and import-download operations. | completed |
| Operations | Error dialog | Native operation result sheet renders typed error details for registry-lock recovery, failed downloads, provider choices, optional recommendation choices, incompatible `.ckan` files, and generic operation errors; failed operation sheets expose Copy Diagnostics, and diagnostics reports/bundles now include structured `operationErrorDetails`, last operation status, changes, event timeline, failed URLs, lock path, command, and suggested action. | completed |
| File workflows | Install from `.ckan` | Native file importer wired to `operations.installCkanFiles` and async `operations.startInstallCkanFiles` for operation-sheet status polling/cancel; sidecar returns typed provider-choice, optional recommendation/suggestion/supporter, incompatible `.ckan` compatibility-warning details, and failed-download retry/skip recovery; the operation result sheet can select a provider, include optional recommendations, explicitly skip optional choices, skip failed downloads, or confirm incompatible local files before retrying with `providerSelections`/`recommendationSelections`/`allowIncompatibleCkanFiles`/`skipDownloadFailures`; disposable CKAN Core coverage verifies compatible local `.ckan` install, unresolved and selected provider/recommendation paths, incompatible-warning and install-anyway paths, registry-lock recovery, and async Sidecar/AppModel polling contracts. | completed |
| File workflows | Import downloads | Native file/folder importer wired to CKAN Core `ModuleImporter` through `operations.importDownloads` and async `operations.startImportDownloads` for operation-sheet status polling/cancel; native options sheet controls install-after-import, preview-before-install, and delete-originals flags; install-after-import can retry while skipping failed downloads; preview-before-install uses the Core `previewBeforeInstall` request flag to import matched archives into cache, return install preview changes without immediate install, stage imported modules, and open the normal change-set preview/apply workflow; disposable CKAN Core coverage verifies matched archive cache import, install-after-import, original-file deletion, registry-lock recovery, and async Sidecar/AppModel polling contracts. | completed |
| File workflows | Export mod list | Mods menu Save Panel exports through CKAN Core formats: plain text, Markdown, BBCode, CSV, TSV | completed |
| File workflows | Export modpack | Native metadata sheet and Save Panel export `.ckan` modpacks through CKAN Core `GenerateModpack`; installed modules can be assigned to Depends, Recommends, Suggests, or Ignore before export, with disposable CKAN Core coverage for relationship-group output | completed |
| Maintenance | Scan unmanaged files | Maintenance menu command wired to CKAN Core `ScanUnmanagedFiles` with result alert; changed scans are persisted to the selected instance registry, AppModel refreshes selected-instance state, and disposable-instance coverage verifies DLL/DLC detection. | completed |
| Maintenance | View unmanaged files | Persistent sidebar maintenance pane and Maintenance menu route list CKAN Core detected unmanaged DLLs/DLCs, resolve unmanaged file paths relative to the selected game instance, and can reveal file-backed entries in Finder; disposable-instance coverage verifies persisted scan/list state for unmanaged DLLs and DLCs, and AppModel coverage verifies reveal path validation. | completed |
| Maintenance | Installation history | Persistent sidebar maintenance pane and Maintenance menu route are backed by `maintenance.history`; lists CKAN history snapshots with mod status, can stage missing available mods into the existing preview/apply flow, can restore snapshot-pinned versions through `installVersions` for preview and apply, and has disposable-instance coverage for snapshot parsing plus registry metadata enrichment. | completed |
| Maintenance | Play time | Persistent sidebar maintenance pane and Maintenance menu route are backed by `maintenance.playTime`/`maintenance.updatePlayTime`; lists all instance timers and saves edited hours through CKAN Core `playtime.json`; disposable-instance coverage verifies load and update persistence. | completed |
| Maintenance | Download statistics | Persistent sidebar maintenance pane and Maintenance menu route are backed by `maintenance.downloadStatistics`; computes cached bytes per host from CKAN Core cache/registry data, renders a native stacked chart with legend plus host table, includes tested SpaceDock/Internet Archive support links, and has disposable-instance coverage for cache host totals. | completed |
| Maintenance | Deduplicate | Maintenance menu command backed by `maintenance.deduplicate`; uses CKAN Core `InstalledFilesDeduplicator`, native confirmation before hard-linking, and native result sheet with captured messages/progress; disposable-instance coverage verifies hard-link deduplication across two KSP instances on macOS. | completed |
| Maintenance | Repair | Maintenance menu command backed by `maintenance.repairRegistry`; calls CKAN Core `Registry.Repair()` for the selected instance, rescans unmanaged files after reindexing, and shows native confirmation/result events; disposable-instance coverage verifies installed-file reindexing, autodetected DLL refresh, and unchanged download-cache contents. | completed |
| Maintenance | Clean cache | Persistent sidebar maintenance pane and Maintenance menu route are backed by `maintenance.cacheInfo`, `maintenance.clearCache`, and `maintenance.purgeCacheToLimit`; shows cache path/files/bytes/free space/limit, opens the folder, purges all after confirmation, purges down to the configured limit, links directly to native cache path/limit editing through Settings, and has disposable-instance coverage for clear and purge-to-limit. | completed |
| Settings | CKAN settings | Native settings window with General, Compatibility, Stability, Hosts, Filters, Launch, Auth, Repositories, and Cache panes; update-on-launch/dev-build/repository-refresh-on-launch/update auto-sort preferences, cache folder/size limit, cache-folder migration choices, compatible game versions, stability tolerance, preferred hosts, install filters, launch command lines, and auth token management are wired through CKAN Core sidecar calls; disposable-instance coverage verifies cache settings, cache move/delete migration choices, compatible versions, stability overrides, preferred hosts, install filters, launch command cloning, legacy launch-command import, and auth token list/add/remove flows | completed |
| Settings | Compatible game versions | Native Compatibility settings pane per selected instance backed by `settings.compatibleVersions`/`settings.updateCompatibleVersions`; shows current/stored game version, known/custom versions, clear/save, refreshes selected-instance state after saving, and has disposable-instance coverage for persisted CKAN compatible-version files | completed |
| Settings | Stability tolerance | Native Stability settings pane per selected instance backed by `settings.stabilityTolerance`, `settings.updateStabilityTolerance`, and `settings.updateModuleStabilityTolerance`; supports overall tolerance plus per-module override add/save/clear, refreshes selected-instance state after saving, and has disposable-instance coverage for persisted overall/module stability settings | completed |
| Settings | Preferred hosts | Native Hosts settings pane backed by `settings.preferredHosts`/`settings.updatePreferredHosts`; shows CKAN Core available hosts by popularity, preserves the Windows `<ALL OTHER HOSTS>` priority marker as a nullable list entry, saves ordered host priority through `IConfiguration.PreferredHosts`, and has disposable-configuration coverage for normalized preferred-host persistence | completed |
| Settings | Install filters | Native Filters settings pane backed by `settings.installFilters`/`settings.updateInstallFilters`; edits global per-game filters and selected-instance filters, exposes CKAN Core `InstallFilterPresets`, appends preset filters to the global list without duplicates, saves through `IConfiguration.SetGlobalInstallFilters` plus `GameInstance.InstallFilters`, and has disposable-instance coverage for normalized global/instance filter persistence | completed |
| Settings | Game command line options | Native Launch settings pane backed by `instances.launchOptions`/`instances.updateLaunchOptions`; edits selected-instance command lines, shows CKAN Core default command lines, reloads from the selected instance, imports legacy `GUIConfig.xml` command-line arguments, and saves/reset-to-defaults through CKAN Core launch options with disposable-instance coverage | completed |
| Settings | Auth tokens | Native Auth settings pane backed by `settings.authTokens`/`settings.addAuthToken`/`settings.removeAuthToken`; lists hosts with masked token previews, adds/updates tokens through `IConfiguration.SetAuthToken`, removes tokens, never returns saved raw token values to Swift, stores macOS secrets in Keychain behind a non-secret config marker, migrates legacy raw config tokens on first successful Keychain lookup while preserving legacy fallback if migration fails, and has disposable-configuration coverage for list/add/remove masking semantics | completed |
| Settings | Plugins | Settings pane is added with explicit native unsupported decision: Windows CKAN plugin support is not yet available in a cross-platform runtime, so MACKAN exposes status and roadmap in Settings | completed |
| Help | User guide | Native macOS Help menu opens the CKAN User Guide | completed |
| Help | Discord/support links | Native macOS Help menu opens CKAN Discord plus selected-game Discord and mod-support links for KSP/KSP2 | completed |
| Help | About/version information | Native About MACKAN sheet shows app version, MACKAN service version, CKAN Core version, protocol, .NET runtime, operating system, and process architecture from `app.version` | completed |
| Help | Report client issue | Native macOS Help menu copies a diagnostic report to the clipboard, saves an attachable diagnostics zip bundle with sidecar health and `app.version` runtime metadata, and opens the CKAN client issue template | completed |
| Help | Report metadata issue | Native macOS Help menu opens the selected game's NetKAN/KSP2-NetKAN metadata issue flow and pre-fills title/body with the selected module when available | completed |
| Updates | CKAN auto-update | Native Help > Check for Updates and General setting `Check for CKAN updates on launch` are backed by `app.checkForUpdates`; startup honors saved update-on-launch/dev-build preferences and opens the native update sheet only for available/failed checks; checks stable or dev-build metadata through CKAN Core `AutoUpdate`, shows current/latest version, release notes, download URLs, and a manual install message. The approved v1 scope uses check + manual install; signed Sparkle/in-app install is tracked as a post-v1 credential-gated release-engineering item. | completed |
| Localization | Existing CKAN translations | Reuse strings where possible; native app starts with English and Russian | completed |
| Accessibility | Windows keyboard/mouse workflows | VoiceOver labels, keyboard navigation, dynamic type where macOS supports it | completed |
| Packaging | Existing DMG opens ConsoleUI | Universal signed/notarized app bundle | completed |
